services:
  frontend:
    build: .
    restart: unless-stopped
    ports:
      - '3000:8080'
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'
